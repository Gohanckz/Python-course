
#Definimos la funcion con la operacion que queramo que se realice
def calculo(numero):
    resultado = (numero-3)**3
    return resultado

#Llamamos la funcion por su nombre y le asignamos el valor que queramos que este lleve dentro.
print(calculo(8))

print(calculo(5))